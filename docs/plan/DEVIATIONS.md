# Deviations log

Facts discovered during the port that contradict the plan, the upstream
documentation, or a reasonable expectation. Per the PLAN.md session
protocol each entry records **symptom**, **evidence**, and
**disposition**. Append new entries at the end; never delete one, since a
resolved deviation is still the reason a design looks the way it does.

---

## D1 — Pillow version changes PNG container bytes without changing pixels

**Milestone**: M0 commit 1 · recorded 2026-07-31

**Symptom**: rerunning `make_fixtures.py` on unchanged inputs reproduced
27 of 29 kit files byte-for-byte, but `v2-small-1376x768.png` differed in
both `fixtures/originals/` and `fixtures/watermarked/`. A file-hash
comparison therefore reported the fixture set as "changed" when nothing
about the image had changed.

**Evidence**: the two files were originally generated with the system
interpreter (Pillow 12.1.1 / numpy 2.4.3); the pinned kit venv has Pillow
12.3.0 / numpy 2.5.1. Recomputing the case under both interpreters:

| | file sha256 | decoded pixel sha256 |
|---|---|---|
| Pillow 12.1.1 | `ca1e1701…` | `54042acd…` |
| Pillow 12.3.0 | `adba435c…` | `54042acd…` |

`max|Δ| = 0` over all 3 170 304 bytes, for the watermarked image as well
(`b164ab68…` under both). The generator is deterministic run-to-run on a
fixed interpreter; only the PNG encoder's filter/compression choice moved.

**Disposition**: no impact on the oracle, and no fix needed.

- Pixel identity, not file identity, is the contract. `decoded_pixel_sha256`
  / `output_decoded_sha256` in the manifest are computed over decoded RGB
  pixels (`_common.decoded_pixel_sha256`), which is also exactly what the
  reference binary consumes via `cv::imread(..., IMREAD_COLOR)`.
- The kit is left as regenerated by the pinned venv, consistent with
  `tools/reference/requirements.txt`.
- This is why M0's acceptance criterion is worded "golden **decoded-pixel**
  hashes match the manifest" rather than comparing files.

---

## D2 — The kit's detection-log regex silently dropped negative scores

**Milestone**: M0 commit 2 · recorded 2026-07-31

**Symptom**: manifest cases were missing whole detection attempts. The
`v1-small-800x600` default run recorded only two detections (both V1) when
the CLI actually performs three (the V2 gate attempt, then the V1 gate,
then V1's remove-internal re-detection). Two of the three negatives
(`clean-800x600`, `clean-2752x1536`) recorded one attempt instead of two.

**Evidence**: the circuit-breaker log line for a clean/mismatched profile
carries a **negative** NCC score —

```
[debug] Detection: spatial=-0.174 < 0.25, rejected
```

— while the inherited regex matched `spatial=([\d.]+)`, which cannot match
a leading `-`. Non-matching lines were skipped rather than raising, so an
attempt that ran was indistinguishable in the manifest from one that never
ran. `TM_CCOEFF_NORMED` is defined over [-1, 1], so negative scores are
normal, not exceptional: after the fix, **3 of 14** cases gained an
attempt — the ones whose breaker line carried a negative spatial score.

| case | breaker spatial values | affected |
|---|---|---|
| `v1-small-800x600` | `-0.174` | yes |
| `clean-800x600` | `0.199`, `-0.094` | yes |
| `clean-2752x1536` | `-0.075`, `0.178` | yes |
| `v1-large-1500x1200` | `0.000` | no — non-negative, the old regex matched |
| `clean-1024x572` | `0.000`, `0.000` | no |
| `v2-large-2752x1536-hard` | `0.178` | no |

**Disposition**: fixed in `gen_golden.py` before any manifest was
committed, so no bad data ever reached `test/data/`.

- All numeric groups in `DETECT_RE`/`REJECT_RE` now accept a leading minus.
- The parser now **fails loudly**: any line containing `Detection:` or
  `Removing watermark at` that matches no known shape aborts the run
  instead of producing a partial manifest.
- `validate_manifest.py` independently recomputes each non-breaker
  confidence from the fusion weights and each breaker confidence from
  `spatial × 0.5`, so a future parsing regression surfaces as a validation
  failure rather than as missing data.

**Precision note**: the breaker line prints only `spatial`, so a
circuit-breaker detection's `confidence` in the manifest is **reconstructed**
(`round(spatial × 0.5, 4)`), not a value the binary printed independently.
Its error against the binary's internal float is bounded by 3e-4 — up to
2.5e-4 inherited from the 3-decimal `spatial`, plus up to 5e-5 from
rounding the product to 4 decimals — comfortably inside the 2e-3
comparison budget. Note also that this confidence is **not clamped**
upstream (`result.confidence = spatial * 0.5` returns directly), which is
why negative confidences are legitimate manifest values.

---

## D3 — `--force-small` is a no-op on large V2 images

**Milestone**: M0 commit 2 · recorded 2026-07-31

**Symptom**: the forced-size run `--force --no-legacy --force-small` on
`v2-large-2752x1536` produces output **byte-identical** to the plain force
run (`output_decoded_sha256` `822acb25…` for both), rather than the
misaligned 36px removal the flag's help text implies ("Force use of 48x48
watermark regardless of image size").

**Evidence**: `remove_watermark()` resolves the template through
`effective_alpha_map(size=Small, V2, 2752, 1536)`, which re-derives
`get_watermark_config(2752, 1536, V2)` — a **dims-based** lookup that
ignores `force_size` — obtaining `logo_size = 96`. Since 96 ≠ the 36px V2
small base, it calls `create_interpolated_alpha(96, 96)`, which returns the
96px source unchanged. The debug line records the contradiction directly:

```
Removing watermark at (2464, 1248) with 96x96 alpha map (size: Small, variant: V2)
```

The other two forced-size runs do diverge, because their dims-based config
resolves to a different template than the forced one: `v2-small-1024x572`
`--force-large` removes a 96×96 template at the 36px position (917, 465),
and `v1-small-800x600` `--force-large` removes a 96×96 template at
(720, 520) on an 800×600 image, overrunning both edges.

**Disposition**: recorded as-is, not corrected — this is the upstream
size-override quirk the plan explicitly requires the port to reproduce
(M0.md commit 2, PLAN.md "forced-size quirk ported as-is"). The manifest
stores each forced-size run's `alpha_map`, `removal_size`,
`removal_variant` and `removal_position` so M4/M5 can assert the quirk
rather than infer it. `validate_manifest.py` deliberately accepts both
outcomes and only flags the self-contradictory combination (identical
output with differing templates).

---

## D4 — `alpha = byte / 255` is not bit-identical to upstream's

**Milestone**: M1 commit 1 · recorded 2026-07-31

**Symptom**: the M1 report claimed the TypeScript division reproduces
upstream's conversion bit for bit. It does not. Upstream's
`convertTo(CV_32FC1, 1.0/255.0)` (blend_modes.cpp `calculate_alpha_map`)
multiplies in a **float32** work type, while this port divides in float64
and stores the result into a `Float32Array`. The two disagree by one ulp
on **126 of the 256** possible byte values.

**Evidence**: the original claim came from a probe that computed *both*
candidates in float64 and only then narrowed to float32 — which compares
two spellings of the same double computation and can only ever agree.
Modelling the work type correctly, and reaching OpenCV's literal
`convertTo` through two independent routes whose scale is exactly 1/255:

| route | vs float32 multiply | vs float64 divide |
|---|---|---|
| `normalize(NORM_MINMAX, alpha=0, beta=1, CV_32F)` | 0 differ | 126 differ |
| `normalize(NORM_INF, alpha=1, CV_32F)` | 0 differ | 126 differ |
| `multiply(uint8, float32 **array**, CV_32F)` | 0 differ | 126 differ |
| `multiply(uint8, **scalar**, CV_32F)` | 126 differ | 0 differ |

The two `normalize` routes agree with each other exactly. `max |Δ| =
5.96e-8`; both endpoints are exact (`v=0` → 0, `v=255` → 1); e.g. `v=3`
gives `0.011764707` from `convertTo` versus `0.011764706` here.

Note the last row: an arithmetic op with a **scalar** operand takes a
double-precision path and lands on the *other* answer. This is the real
lesson — the multi-route corroboration that validated the quantization
dump (D-none; all routes agreed there) does **not** extend to scaled
conversions, where the work type depends on whether the operand is a
scalar or an array. Any future cv2 oracle involving a scale factor must
say which form it used.

**Disposition**: keep the float64 division; no code change.

- CLAUDE.md rule 4 already states the contract: C++ computes in float32,
  JS in float64, and "this is acceptable — the contract with golden
  outputs is ±1 per 8-bit channel on restored pixels, not bit-identical
  floats". Decisions and geometry remain exact; only float width moves.
- Downstream budget, for M2. Removal is
  `orig = (w − 255α) / (1 − α)`, so `∂orig/∂α = (w − 255)/(1 − α)²`. For a
  genuinely watermarked pixel `w = 255α + (1 − α)·orig`, which collapses
  that to `(orig − 255)/(1 − α)`. With the remove-path clamp `α ≤ 0.99`
  the amplification is at most `255 / 0.01 = 2.55e4`, so the worst-case
  error is `5.96e-8 × 2.55e4 ≈ 1.5e-3` of one uint8 level — three orders
  of magnitude inside the ±1 tolerance.
- The residual risk is not magnitude but position: a golden pixel whose
  exact value sits within 1.5e-3 of a rounding boundary could quantize
  differently. If an M2 golden comparison ever fails by exactly 1 on
  isolated pixels, check this before suspecting the blend math.
